"""Cross-cutting infrastructure shared by every domain module.

Lives at the bottom of the layered architecture (`ARCHITECTURE.md` §2).
Modules depend on `core`; `core` depends on nothing inside the project.
"""
