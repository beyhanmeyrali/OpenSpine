"""Identity, authentication, RBAC, and audit.

Tables prefixed `id_*`. See `docs/identity/` for the specification.

Implementation lands in v0.1 §4.2 (identity core) and §4.3 (RBAC + auth-object
engine). This package is currently a placeholder so the layered architecture
is explicit from the first commit.
"""
